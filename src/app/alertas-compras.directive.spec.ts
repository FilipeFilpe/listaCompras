import { AlertasComprasDirective } from './alertas-compras.directive';

describe('AlertasComprasDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertasComprasDirective();
    expect(directive).toBeTruthy();
  });
});
